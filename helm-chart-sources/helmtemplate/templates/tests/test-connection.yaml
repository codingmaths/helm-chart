apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webengage-helmtemplate.fullname" . }}-test-connection"
  labels:
    {{- include "webengage-helmtemplate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webengage-helmtemplate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
